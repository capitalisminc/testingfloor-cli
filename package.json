{
  "name": "@testingfloor/cli",
  "version": "0.1.0",
  "description": "Minimal CLI for uploading game builds to Testing Floor.",
  "type": "module",
  "bin": {
    "testingfloor": "./bin/testingfloor.js"
  },
  "scripts": {
    "build": "esbuild src/action.js --bundle --platform=node --format=cjs --target=node20 --outfile=dist/action.cjs",
    "lint": "node --check bin/testingfloor.js && node --check src/action.js && node --check src/action-core.js && node --check src/cli.js && node --check test/cli.test.js && node --check dist/action.cjs",
    "prepack": "npm run build",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "action.yml",
    "bin",
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "keywords": [
    "testingfloor",
    "gamedev",
    "ci",
    "build-upload"
  ],
  "license": "MIT",
  "dependencies": {
    "zip-stream": "^7.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
